# Required
DIFY_API_KEY=

# Optional (default: https://api.dify.ai/v1)
DIFY_API_BASE=https://api.dify.ai/v1

# Knowledge Base IDs (optional for single-run; required for run_evaluation.py batch compare)
DATASET_ID_GENERAL=
DATASET_ID_PARENT_CHILD=
DATASET_ID_QA=

# Reranking (optional; must match Dify "System Model Settings")
RERANK_PROVIDER_NAME=local
RERANK_MODEL_NAME=bge-reranker-base

# Gold matching mode:
# - doc_id: use gold_doc_id (single dataset only)
# - doc_name: use gold_doc_name (recommended for comparing multiple datasets)
# - auto: prefer gold_doc_id, fallback to gold_doc_name
GOLD_MATCH_MODE=doc_name

